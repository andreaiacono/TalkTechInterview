package it.jugmilano.problems;

/**
 * Given an array of integers and a number, find
 * the pair of numbers in the array that sum to
 * the number, or an empty array if any.
 * If more than one pair satisfies the requirements,
 * return the one with the lowest value.
 *
 * Example:
 * array = {1, 2, 3, 4, 5, 6, 7}, sum = 7
 *
 * There are three pairs that satisfy the requirements:
 * [1, 6], [2, 5], [3, 4]
 *
 * The one with the lowest value is the correct result:
 * [1,6]
 *
 *
 */
public class PairSum {

    static int[] pairSum(int[] n, int sum) {
        return new int[] {};
    }
}
